#!/usr/bin/env python3
import sys

from drone_marathon import DroneMarathon, MarathonCliError


if __name__ == '__main__':
    try:
        DroneMarathon().run()
    except MarathonCliError as e:
        sys.exit(e.returncode)
    sys.exit(0)
